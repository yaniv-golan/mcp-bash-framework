#!/usr/bin/env bash
set -euo pipefail

# Get the directory of this script
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"

# Execute the main runner with this example's ID
exec "${REPO_ROOT}/examples/run" "04-ffmpeg-studio"


