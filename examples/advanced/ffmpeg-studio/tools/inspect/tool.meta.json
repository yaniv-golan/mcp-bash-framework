{
  "name": "inspect_media",
  "description": "Inspects a media file to retrieve metadata (codec, duration, resolution) using ffprobe.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Absolute or relative path that must resolve inside an allowed media root (configure via config/media_roots.json)."
      }
    },
    "required": ["path"]
  },
  "outputSchema": {
    "type": "object",
    "description": "ffprobe JSON output containing format and streams."
  }
}

